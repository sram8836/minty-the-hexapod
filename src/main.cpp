#include "Brain.h"
#include "StateTransmitter.h"
#include "Leg.h"

int main() {
    Brain hexapod = Brain();
    // hexapod.updateVelocity(50.0f, 50.0f, 0.0f);

    // StateTransmitter* txr = new StateTransmitter();
    // Leg leg = Leg(0, M_PI/4, txr);
    // leg.saveTrajectory();

    return 0;
}
